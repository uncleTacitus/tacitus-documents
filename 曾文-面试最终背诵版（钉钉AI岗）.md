# 曾文 - 面试最终背诵版（钉钉 AI 岗位）

> 目标：一份文档覆盖自我介绍、核心项目、通用技术、钉钉岗位定向、应急话术。  
> 使用方式：按优先级 P0 → P1 → P2 → P3 背诵，不要平均用力。  
> 核心原则：任何问题都能拉回“我做过的 4 个项目故事”里回答。

---

## 背诵优先级说明

| 优先级 | 含义 | 内容 |
|--------|------|------|
| **P0** | 周日必须背完，面试开场和核心项目 | 万能框架、自我介绍、4 个项目完整故事、钉钉场景专属问题 |
| **P1** | 高频追问，必须能脱口而出 | 项目下的 Q&A、ReAct、RAG、MCP、工具注册 |
| **P2** | 覆盖性内容，能说出要点即可 | 多 Agent、Agent 评估、记忆系统、红色技能降级 |
| **P3** | 应急/兜底，背住金句 | 非科班、离职原因、反问、不会的问题 |

**背诵策略**：
- 先背 P0，再背 P1。
- P2 看熟关键词，能说 30 秒。
- P3 背金句，不展开。

---

## 一、万能回答框架（P0，所有问题先用这个）

任何技术问题，按这 4 步答：

1. **一句话定位**：这是什么技术/概念；
2. **拉回项目**：我在哪个项目里用过/处理过类似问题；
3. **讲三点关键**：第一、第二、第三；
4. **一句金句收尾**。

### 示例：被问“RAG 怎么做”

> RAG 就是先查外部知识，再让 LLM 基于证据回答。  
> 我在企微机器人里做过一个轻量版 RAG：用户问销售额，我不让 LLM 现编，而是先匹配意图，再直接查数据库。  
> 第一，把用户问题分类，看是不是数据查询；第二，数据查询命中规则就直接读 PostgreSQL 出图；第三，没命中但有数据意图就记录补规则，闲聊才走 LLM。  
> 所以我的理解是：**生产级 AI 不是让 LLM 做所有事，而是让 LLM 只做它该做的事。**

---

## 二、自我介绍（P0，开场 2 分钟）

### 通用版

> 面试官你好，我叫曾文。  
> 我有两条线：技术线和业务线。  
> 技术上，我是 Python + Java 双栈后端。Python 这边，我用 FastAPI + SQLAlchemy 2.0 异步栈做过一个上线的家装分销系统，涉及多角色权限、线索状态机、微信支付资金一致性。Java 这边，我系统学习并实践过 Spring 微服务、消息队列、分布式事务等原理。  
> 最近一年我重点学习 AI Agent 方向，深入理解了 PaiAgent（工作流编排平台）和 PaiCLI-Python（终端 Agent）的核心实现，涉及 ReAct 循环、工具注册、MCP、RAG、记忆系统、多 LLM 提供商集成。  
> 业务上，我在博士眼镜做过两年零售运营，从销售到供应链到库存都跑过。这段经历让我对“流程”有很具象的理解，做 Agent 工作流引擎时，我比纯技术出身的同学更清楚边界在哪、异常怎么处理。  
> 我的优势就是三点结合：后端工程落地能力 + AI Agent 核心原理理解 + 业务流程思维。我不跟算法岗拼论文，我跟工程岗拼 Agent 系统的落地能力。  
> 谢谢。

## 三、核心项目故事（P0-P1）

### 项目 1：大宅门推客系统（P0）

#### 完整故事（90 秒）

> 大宅门推客系统是我们公司的一个家装行业线索分销平台。简单说就是推荐官把客户拉进来，经过店小二、设计师、定金、签约、开工这几个环节，每个环节都有佣金和奖励。  
> 这个项目我从 v1 重构到 v2。v1 是 Django 同步框架，权限散在接口里写 if-else，状态用一个字符串字段表示，很多业务情况表达不清楚。v2 我换成 FastAPI + SQLAlchemy 2.0 异步栈，重点解决三个问题：权限、状态机、资金安全。  
> 第一，权限。v1 每个接口自己判断角色，改一个角色名要改几十处。v2 我把权限收敛成三层装饰器：RequirePermission 做角色鉴权，RequireCertified 做资质认证，RequireIdentityVerified 做实名校验，再加一个 CompanyScope 做公司级数据隔离。总公司用户登录后，自动聚合所有分公司数据。  
> 第二，状态机。v1 的线索状态是一个字符串，比如“跟进中”。问题是它不能同时表达“已分配”“已进店”“已签约”这些独立维度，而且奖励触发和状态耦合，主管恢复线索后再签约，系统可能重复发奖。v2 我把状态改成 17 位 JSONB 状态位，每个业务状态独立。奖励是否发放也单独成位，一旦置 1 就不会再变。这样主管怎么恢复，奖励都不会重发。  
> 第三，资金安全。提现流程我设计成先写库再调微信。因为一旦微信把钱转出去，但系统没记录，后续就可能重复转账，钱就白给出去了。 所以我先 commit 数据库生成一条提现记录，保证状态可追溯，再调微信接口。另外我加了三层幂等：
> 回调幂等：微信回调可能重复发，我根据业务单号判断已经处理过的直接返回。
> 状态幂等：同一个提现请求已经处理中或成功，就不允许重复发起。
> 金额校验：微信回调金额和本地记录一致才更新成功，防止金额对不上。
> 所以这个项目让我把 Python 异步工程、业务状态机、资金安全三件事同时落地在一个真实系统里。

#### Q1：你做过最复杂的系统是什么？（P0）
直接讲上面完整故事。

#### Q2：权限系统怎么设计？（P1）
> 我在大宅门里做了三层守卫 + 数据隔离。  
> 第一层 RequirePermission 做角色白名单；第二层 RequireCertified 做资质认证，比如设计师要通过审核；第三层 RequireIdentityVerified 做实名认证，主要在提现前用。再加上 CompanyScope 做公司级数据过滤，总公司自动聚合所有分公司数据。  
> **金句：权限不是散落在每个接口的 if-else，而是一串依赖注入链，谁加谁摘都一目了然。**

#### Q3：状态机怎么设计？（P1）
> 大宅门线索状态不是线性字符串，而是 17 位 JSONB 状态位。  
> 每个业务状态独立，比如 verified、assigned、following、visited、deposited、signed。奖励是否发放也单独成位，设置后不可回退。  
> 好处是：一个线索可以同时是“已分配”和“已签约”；主管恢复线索后重新签约，奖励位已经置 1，不会重复发奖。  
> **金句：当业务状态不是线性流转而是多维组合时，位图比状态字符串更灵活，特别是状态变化会触发资金操作时。**

#### Q4：微信支付怎么保证资金安全？（P1）
> 我做的是“先 commit 再调微信”。  
> 具体流程：先冻结余额、创建提现记录、commit 到数据库，再调用微信转账。微信失败了立即解冻退款。  
> 幂等有三层：回调用 notify_id 去重；处理时加行锁，状态已经是 success 就跳过；金额不一致直接告警不自动修。  
> 还有一个每 5 分钟的对账任务，主动查微信确认最终状态。  
> **金句：支付系统做好三层事：并发不超扣、回调不重复处理、余额不一致时宁可停等人工也不自动修。**

#### Q5：v1 到 v2 怎么迁移？（P2）
> 我写了一个 17 步的 Alembic 迁移脚本，把旧数据逐步导入新 schema。  
> 迁移前我做了字段映射规则，迁移后做了抽样校验和总量校验，确保新旧系统数据一致。整个过程是一次性完成，没有回滚。  
> **金句：生产迁移最重要的是可验证，不是一次写完，而是每一步都能比对。**

---

### 项目 2：AI 数据图表直查（P0，最贴钉钉岗位）

#### 完整故事（90 秒）

> 我在公司企微机器人里做了一个数据图表直查功能。员工在群里问“本月销售额多少”“今天新增多少线索”，机器人直接返回一张图表。  
> 核心思路是规则前置，LLM 兜底。我把查询分成三层。  
> 第一层是高频数据查询，比如“本月销售额”“今日新增线索”。我用正则做意图匹配，命中后直接查 PostgreSQL，生成 SVG 或 PNG 返回。这一层完全不调用 LLM，所以响应快、无幻觉。  
> 第二层是规则没命中，但听起来又像查数据的。比如某个新的复合指标。这时候我不让 LLM 现编 SQL，而是回复“这个查询暂时不支持，已记录”，后台异步补规则。  
> 第三层是闲聊，比如“早上好”，才走 OpenClaw Gateway 的 LLM SSE 流式回复。  
> 另外我还做了分级缓存，不同指标 TTL 不同；图表上会标注“数据更新时间”，让用户自己判断新鲜度。  
> 这个设计的 trade-off 是开发侧要维护规则表和 SQL，但换来了低延迟和高确定性。

#### Q1：你做过什么 AI 项目？（P0）
直接讲上面完整故事，强调这是为企业 IM 机器人做的智能问答 + 数据分析。

#### Q2：怎么减少 LLM 幻觉？（P1）
> 我的做法是“规则前置，LLM 兜底”。  
> 高频数据查询不走 LLM，直接读数据库；不确定的查询明确说“不支持”，不硬答；只有闲聊才交给 LLM。  
> 另外图表标注数据更新时间，让用户自己判断。  
> **金句：生产级 AI 不是让 LLM 做所有事，而是让 LLM 只做它该做的事。**

#### Q3：意图匹配怎么做的？（P1）
> 我用正则表匹配中文查询，比如“本月销售额”“今日新增线索”。  
> 规则按“具体优先于笼统”排序，比如“个人业绩”要排在“整体 KPI”前面，避免“李海芳本月业绩”被整体看板抢走。  
> 匹配不上但有数据关键词，就返回“暂时不支持，已记录”。  
> **金句：正则顺序就是业务优先级，越具体的问题越要靠前。**

#### Q4：怎么保证 SQL 只读、安全？（P1）
> 第一道防线是命中规则后走预置 SQL，不拼接写操作。  
> 第二道是运行期校验，比如 SQL 必须以 SELECT 开头，禁止 INSERT/UPDATE/DELETE/DROP 等关键字。  
> 第三道是数据库账号最小权限，只读。  
> **金句：数据查询的信任链：代码层校验 + 协议层只读 + 账号层最小权限。**

#### Q5：这算不算 RAG？（P1）
> 算，是一个轻量版 RAG。  
> RAG 的核心是“先检索知识，再让 LLM 生成”。我这里检索的是规则表和 SQL，而不是向量库。但思想一致：先查资料，再决定怎么答。  
> **金句：RAG 不一定非得用向量，只要是先查资料再回答就是 RAG。**

#### Q6：缓存和性能怎么设计？（P2）
> 我们为不同指标设置了分级缓存 TTL：今日实时指标 5 分钟，周/月趋势 15 分钟，分布/状态类 30 分钟。cache_key 把参数也算进去，避免互相覆盖。  
> 用户说“刷新/最新/重新查”时强制穿透缓存。个人业绩查询较慢时，先回“正在查询，请稍候”，再后台跑 SQL。  
> **金句：在有限硬件下，缓存和规则是延迟的敌人；LLM 只留给真正需要推理的对话。**

#### Q7：怎么支持“查具体的人”？（P2）
> 把人名、时间、指标三要素从问法里拆出来。先模糊匹配用户表，再按参数查业绩。  
> 没找到返回“未找到该员工”；找到多个返回候选列表；找到唯一再按时间范围查负责线索、签约、开工、定金、进店、跟进等。  
> **金句：把自然语言先翻译成结构化参数（人名+时间+指标），再交给预置 SQL，而不是让 LLM 去理解。**

---

### 项目 3：PaiAgent 工作流平台（P1）

#### 完整故事（90 秒）

> PaiAgent 是一个可视化的 AI Agent 工作流编排平台。用户可以在画布上拖拽节点，比如输入节点、LLM 节点、工具节点，把它们连成一个工作流。  
> 我重点理解并参与了它的双引擎设计、ReAct Agent 节点、工具注册和一次关键 Bug 修复。  
> 双引擎是指 DAG 引擎和 LangGraph 引擎。DAG 适合无环线性流水线，拓扑排序后顺序执行，简单可控。LangGraph 适合需要循环和条件判断的复杂场景，比如“判断是否需要搜索 → 搜索 → 再判断是否需要继续拆分”。两个引擎共享同一套 NodeExecutor，所以新增节点类型时两个引擎都能用。  
> ReAct Agent 节点是一个 for 循环。每轮把工具 schema 传给 LLM，LLM 决定是返回最终答案还是调工具。调完工具把结果塞回 messages，继续下一轮。最多 20 步强制结束，防止死循环。  
> 工具注册方面，启动时 AgentToolRegistry 会扫描内置工具，也会从 mcp_tool_config 表加载用户配置的 MCP 工具，全部存在 registry 里。节点配置哪些工具名，就给 LLM 暴露哪些工具描述。  
> 我修过一个 Bug。VolcengineAgentPlanClient 处理 HTTP 错误时，原代码直接读 conn.getErrorStream()，但这个流可能是 null，导致 NPE，把真实的 embedding 错误盖住了。我加了 null 判断，后面排查问题从盲猜变成看真实状态码。

#### Q1：什么是 Agent / 智能体？（P1）
> Agent 是能自主决策、调用工具、根据反馈调整的系统。  
> 在 PaiAgent 里，ReAct Agent 节点就是这样一个循环：LLM 每步决定调什么工具，工具结果反馈回去，LLM 再决定下一步，直到完成任务或达到步数上限。  
> **金句：Agent 不是一次想对，而是能根据反馈调整，直到找到正确答案或达到上限。**

#### Q2：DAG 和 LangGraph 怎么选？（P1）
> DAG 适合固定流水线，无环、线性、好调试；LangGraph 适合有循环和条件判断的复杂任务。  
> 两者共享同一套 NodeExecutor，LangGraph 通过 NodeAdapter 适配现有节点。  
> **金句：PaiAgent 不是两套实现，而是共享节点执行器，只有调度层不同。**

#### Q3：工具怎么注册和发现？（P1）
> 启动时 AgentToolRegistry 扫描内置工具，也从 mcp_tool_config 表加载 MCP 工具，全量存在 registry。  
> 运行时 ReActAgentNodeExecutor 按节点配置的工具名，从 registry 查出实例，把 name/description/inputSchema 拼进 LLM 的 tools 参数。  
> 一个小限制是前端 EditorPage 硬编码展示，后端其实已经支持 MCP 扩展了。  
> **金句：PaiAgent 后端已经支持 MCP 扩展，限制主要在前端展示层。**

#### Q4：多 LLM 提供商怎么集成？（P1）
> PaiAgent 用 ChatClientFactory 封装差异。  
> 普通模型走 OpenAI-compatible 接口；火山引擎的 agent_plan 有特殊调用方式，单独封装 VolcengineAgentPlanClient。  
> 上层业务只调用 createClient()，不关心具体 provider。  
> **金句：多 LLM 集成的关键是把调用差异收敛到工厂层。**

#### Q5：Skill 技能系统是什么？（P1）
> Skill 是 PaiAgent 里的一种任务模板，分三层加载：summary 用于 LLM 选择，detail 用于规则约束，reference 用于深度执行。  
> 先让 LLM 看 summary 选 Skill，再读 SKILL.md 约束行为，执行中需要时再读 reference 文档。  
> 这样避免一次性把所有技能文档塞进 prompt 导致 context 爆炸。  
> **金句：Skill 不是越大越好，而是按需分层加载。**

#### Q6：讲一个你解决的复杂 Bug（P1）
> 讲 VolcengineAgentPlanClient 的 NPE Bug。  
> 原代码：`(status >= 200 && status < 300 ? conn.getInputStream() : conn.getErrorStream()).readAllBytes()`。  
> 问题：HTTP 错误时 getErrorStream() 可能为 null，直接 NPE，把真实错误盖住。  
> 修复：先判空，再读取，同时处理 text.isBlank()。  
> 结果：重新运行后，直接定位到 memory_write 的真实错误是 embedding 接口配置缺失。  
> **金句：这个改动让所有 Agent Plan 工具调用的排查成本从盲猜变成看真实的 HTTP 状态码。**

#### Q7：PaiAgent 的 ReAct Agent 运行时流程是怎样的？（P1）
> PaiAgent 的 ReAct Agent 在 ReActAgentNodeExecutor 里是一个 for 循环，默认最多 5 步，上限 20 步。  
> 每步：executor 把从 AgentToolRegistry 查出来的工具 schema 拼进 LLM 调用的 tools 参数。LLM 返回一个 JSON 决策，executor 解析，提取 action（工具名）和 action_input（参数）。  
> 然后判断：如果 LLM 的 finish_reason == 'stop'，直接返回最终答案；如果 finish_reason == 'tool_calls'，就去 registry 查对应工具实例来执行，结果再塞回 messages，继续下一轮循环。  
> 步数到了上限还没结束，就强制终止，返回当前结果。  
> **金句：ReAct 的智能来自循环 + 工具反馈。**

---

### 项目 4：PaiCLI-Python 终端 Agent（P1）

#### 完整故事（90 秒）

> PaiCLI-Python 是一个给程序员用的终端 Agent CLI。你跟它说“给这个项目加一个用户登录功能”，它会自己读代码、改代码、跑测试、生成提交信息。  
> 它的核心有几个部分。  
> 第一是 ReAct Loop。一个 while 循环，每轮把 messages 和工具 schemas 传给 LLM，LLM 决定要不要调工具。如果有 tool_calls，就找到对应函数执行，把结果按 OpenAI 格式塞回 messages，继续下一轮。最多跑 max_steps 步。  
> 第二是工具注册。工具函数用 @tool 装饰，自动从函数签名和文档生成 JSON Schema。启动时注册到 ToolRegistry，运行时把 definitions 传给 LLM。  
> 第三是 MCP 连接。支持 stdio 和 Streamable HTTP 两种传输。stdio 适合本地工具进程，HTTP 适合远程服务。连上 MCP Server 后，把它提供的工具转成内部格式一起给 LLM。PaiCLI 自己也能作为 MCP Server 暴露内置工具。  
> 第四是记忆和快照。短期记忆是当前对话 history；长期记忆存到 SQLite，跨会话记住用户偏好；每次运行前后自动快照，改坏了可以 restore。  
> 另外写工具是串行执行并加审计，bash 这类高危操作需要 HITL 审批。

#### Q1：MCP 是什么？（P1）
> MCP 是连接 LLM 应用和外部工具服务的标准协议。  
> 在 PaiCLI 里，MCP 让外部 Server 提供的工具能像内置工具一样被 LLM 调用。比如一个 PostgreSQL MCP Server，Agent 就能查数据库。  
> **金句：MCP 让 PaiCLI 的能力边界从内置工具扩展到整个 MCP 生态。**

#### Q2：MCP 和 Function Calling 什么区别？（P1）
> Function Calling 是 LLM 输出调用参数的能力，函数通常写在应用内部。  
> MCP 是一个协议，工具放在独立的 Server 里，可以被多个 Agent 复用。  
> Function Calling 更轻量，适合单应用内部；MCP 更重，但适合共享外部能力。  
> **金句：Function Calling 是 LLM 怎么调函数，MCP 是怎么让 LLM 安全地调用外部系统。**

#### Q3：工具注册怎么做的？（P1）
> PaiCLI 用装饰器模式。函数加 @tool，自动生成 JSON Schema，启动时注册到 ToolRegistry。  
> 运行时把工具定义传给 LLM，LLM 根据 name 和 description 选择调用。  
> 对比 PaiAgent，PaiCLI 更强调本地 CLI 的权限、并发和审批。  
> **金句：PaiCLI 的工具系统不是简单地让 LLM 调用函数，而是按读写分离做并发控制、按危险等级做审批、按审计做可追溯。**

#### Q4：记忆系统怎么设计？（P2）
> 分四层：项目级文件约定、SQLite 长期记忆、对话历史短期记忆、快照回滚。  
> 项目级文件放静态约定；长期记忆存关键事实跨会话复用；短期记忆维持当前对话上下文；快照用于改坏代码后 restore。  
> **金句：终端 Agent 的信任链：工具可审计、写操作可回滚、长期记忆可追溯。**

#### Q5：写操作怎么保证安全？（P2）
> 读工具并发执行，写工具串行执行，防止文件冲突。  
> bash 等高危操作需要 HITL 人工审批。  
> 每次工具执行写审计日志，配合 snapshot 可以回滚。  
> **金句：终端 Agent 的高危操作必须有审批和回滚两条生命线。**

---

## 四、通用技术问题（P1-P2）
### 你平时怎么用 Agent CLI 做开发？有什么心得？
> 一句话定位： 我把 Kimi CLI 当作"有明确边界的协作者"：先写文档对齐需求，再用 Agent 做实现，最后靠测试和审查守住质量。
> 
> 口述答案（90-120 秒）：
> 
> "我日常用 Kimi CLI 辅助开发，核心是'文档为先、人机协作、测试兜底'。
> 
> 文档为先：任何改动前先在项目里维护 AGENTS.md 和 PRD.md，把背景、约束、验收标准写清楚。这既是给 Agent 看的上下文，也是给人看的决策留痕。
> 
> 需求澄清 + 影响面评估：改之前我会让 Agent 帮我review需求，问自己三个问题：会不会影响现有功能？有没有安全/权限风险？会不会破坏已有的测试或接口契约？
>
> 写 PRD，对齐需求文档：把需求拆成可验收的任务，输出 PRD。PRD 和 AGENTS.md 互相校验，避免跑偏。
>
> 拆 issue 到 Plane：把 PRD 里的任务拆成 Plane 上的具体 issue，作为开发追踪单元。
>
> Agent 工作流开发：按 Plane issue 让 Kimi CLI 做代码实现。过程中我会指定使用对应的 skill 和 MCP 工具，比如用 read_file/grep 先读代码，用 write_file 改代码，用 bash 跑测试。
>
> 红绿灯测试 + 回归审查：先跑原有测试确认没坏（绿灯保持），再写新功能测试让它变绿。最后 diff 检查代码是否符合 PRD，必要时回滚快照。

> 心得就三点：
> 
> Agent 最擅长'边界清晰的实现'，不擅长'拍板架构'。
> 文档不是写完就丢，是开发前对齐、开发中对标、开发后审查的基准。
> 安全阀永远在人手里：高危操作走 HITL 审批，改坏代码用 snapshot 回滚。"

> 面试金句：
> 
> "我把 Agent 当作实习生：给它清晰的上下文和验收标准，它能把代码写得很快；但上线前的判断权和责任必须在我。"

> 可能追问：
> 
> Agent 改坏了代码怎么办？答：Agent CLI 每次运行有 pre-turn/post-turn 快照，可以直接 /restore 回滚；另外我会先 commit 或 stash 再让 Agent 动手。
> 
> 怎么保证 Agent 不改飞？答：先写 PRD/AGENTS.md 定约束，开发中限制可用工具，开发后跑测试 + diff 审查。
> 
> 什么任务适合 Agent，什么不适合？答：适合边界清晰、可测试、重复性高的任务，比如补接口、写测试、改字段；不适合安全敏感、需要跨系统权衡的架构决策。
> 
> 你用了哪些 skill 和 MCP？答：根据项目用对应的 skill（比如某个框架的 SKILL.md），MCP 主要是代码搜索、文档检索、测试运行类工具。

### G1：ReAct vs Plan-and-Execute（P1）
> ReAct 是“边想边做”，每步根据观察调整；Plan-and-Execute 是“先计划再执行”，适合多步骤任务。  
> ReAct 适合需要灵活调整的任务，比如错误重试或动态工具选择。Plan-and-Execute 适合步骤明确、可以预先分解的任务，比如“先查天气，再查路线，再生成报告”。  
> PaiCLI-Python 同时支持两种模式，需要强可控性时用 Plan-and-Execute，需要灵活推理时用 ReAct。  
> **金句：ReAct 不是一次推理，而是思考 → 行动 → 观察 → 再思考的闭环。**

### G2：RAG 全流程 + Chunk 策略（P1）
> RAG 全称 Retrieval-Augmented Generation，解决 LLM 知识滞后和幻觉。  
> 完整流程：文档解析 → Chunk 切分 → Embedding → 向量库存储 → 检索 top-k → 拼进 prompt → LLM 生成。  
> Chunk 策略：固定字符/token 最简单；递归切分保留段落边界；语义切分质量高但成本高；结构切分适合代码和 Markdown。  
> PaiCLI 里的 CodeIndex 是轻量版：按代码行做 chunk，存 SQLite，查询用关键词 LIKE 匹配。  
> **金句：RAG 不一定是向量检索，只要是先查资料再回答就是 RAG。**

### G3：多 Agent 协作模式（P2）
> 三种常见模式：顺序接力、主从分配、圆桌讨论。  
> 顺序接力像流水线；主从分配是 Manager 拆任务、Worker 并行做、Reviewer 检查；圆桌讨论是多角色围绕同一问题反复讨论。  
> 核心是任务拆分、状态共享、冲突解决。  
> **金句：多 Agent 不是堆数量，是任务拆清楚、状态管明白、冲突有规则。**

### G4：Agent 怎么评估（P2）
> 四个维度：任务完成度、工具调用正确性、输出质量、成本效率。  
> 具体做法：准备测试集，跑多次看成功率和平均步数；失败 case 分类是工具描述问题、LLM 理解问题还是环境问题。  
> 在 PaiAgent 里可以通过 DebugDrawer 看每步执行结果；在 PaiCLI 里可以检查 snapshot 和审计日志。  
> **金句：评估 Agent 不是评估 LLM，是评估一个能自主调用工具完成任务的系统。**

### G5：Context 窗口快满了怎么办？（P2）
> 五种策略：短期记忆截断、长期记忆摘要、关键信息抽取、RAG 外挂、工具结果压缩。  
> PaiCLI 用快照 + 长期记忆结合：Run 前后自动快照，关键事实持久化到 SQLite，context 里只保留必要信息。  
> **金句：context 管理的核心是保留决策所需信息，而不是保留所有历史。**

### G6：非科班非算法，为什么能做 Agent 后端？（P2）
> Agent 后端不是算法岗，重点是工程落地能力。  
> 我的优势有三点：双栈工程能力、AI 工程实践、业务视角。  
> 我不跟算法岗拼论文，我跟工程岗拼 Agent 系统的落地能力。  
> **金句：Agent 后端拼的不是发论文，而是把 ReAct、RAG、MCP、工具系统真正跑在生产环境里。**

---

## 五、钉钉 AI 岗位定向补充（P0-P1）

> 这一节专门对应你面试的钉钉 AI 岗位 JD。原来的 4 个故事不用重背，把关键词从“企微”转译成“钉钉”即可。

### 5.1 企微经验转钉钉场景的一句话（P0）
> “我没有直接做过钉钉，但做过企业微信机器人的集成和开发。两者形态很接近：开放平台注册应用、配置事件回调、接收消息、调用业务接口、发送消息卡片。我觉得技术底层通用性很强，快速上手钉钉应用没问题。”

### 5.2 项目往钉钉场景一句话转译（P1）

| 项目 | 钉钉场景说法 |
|------|------------|
| 大宅门 | “这是一个涉及销售线索、分配、跟进、审批、支付的流程自动化系统，和钉钉里的审批流程、费用报销很接近。” |
| AI 直查 | “这就是为企业 IM 机器人做的智能问答 + 数据分析，链路是意图匹配 → 查数据库 → 生成图表 → 发消息卡片。钉钉机器人里的数据问答也是这个路径。” |
| PaiAgent | “这是一个可视化 AI 工作流平台，比如可以搭一个流程：钉钉会议纪要 → 提取待办 → 调用钉钉任务接口 → 发消息通知负责人。” |
| PaiCLI | “这是一个终端 Agent，程序员可以用自然语言帮你开发和调试钉钉应用。” |

### 5.3 钉钉场景专属问题（P1）

#### Q1：你做过钉钉应用吗？
> “没有直接做过钉钉，但做过企业微信机器人的集成和开发。两者形态很接近：开放平台注册应用、配置事件回调、接收消息、调用业务接口、发送消息卡片。我觉得技术底层通用性很强，快速上手钉钉应用没问题。”

#### Q2：你怎么看待低代码？
> “低代码平台对于标准化、重复性高的场景是很好的工具，可以让业务人员快速搭建。但复杂的 AI 工作流、精细的意图匹配、幻觉防御、数据安全控制，代码级开发更可控。我会根据场景选择：简单重复的用低代码，复杂业务和 AI 逻辑用代码级平台。”

#### Q3：如果让你做钉钉会议自动纪要，你怎么设计？
> “我会分成三个模块。第一，会议音频录入，用钉钉会议录音或音频接口；第二，ASR + LLM 做语音转写和要点提取，用 Prompt 工程约束输出格式；第三，提取出待办事项后，调用钉钉待办/任务接口，发消息通知负责人。  
> 关键要做好几点：纪要要点的准确性、待办分配的准确性、敏感信息脱敏、用户反馈收集。”

#### Q4：你怎么让钉钉机器人安全地查数据？
> “第一，预置 SQL，不让 LLM 现编 SQL；第二，运行期校验，SQL 只能以 SELECT 开头，禁止写操作；第三，数据库账号最小权限，只读；第四，答案里标注数据更新时间，避免用户误以为是实时最新数据。”

#### Q5：你怎么看待模型选型？
> “我从三个角度看：任务类型、成本、效果。文本生成和问答用通用大模型；数据检索增强用嵌入模型打向量；语音转写用语音专用模型。在 PaiAgent 里我用 ChatClientFactory 做了多 provider 集成，上层业务不关心具体是哪家模型，换模型只需改配置。”

### 5.4 弱项降级话术（P2）

| 被问到 | 回答 |
|--------|------|
| 钉钉开放平台 | “没直接做过钉钉，但做过企业微信机器人，形态很接近，快速上手没问题。” |
| 低代码 | “低代码平台适合标准化场景，复杂 AI 流程需要代码级可控。” |
| 模型微调 | “我目前实战主要用 Prompt 工程、RAG 增强、工具调用优化效果，模型微调没有在生产环境深度做过，但原理和评估方法我了解。” |
| Docker | “Docker 我了解，自学环境用过，上一个生产项目用的是传统部署。如果需要很快能上手。” |

### 5.5 钉钉岗位反问（P2）
1. 贵公司目前钉钉 AI 主要落地在哪些业务场景？比如智能问答、自动纪要还是流程自动化？
2. 如果我入职，第一个项目大概是什么角色？是帮客户做钉钉应用定制还是做内部平台？
3. 团队现在的技术栈是偏 Python 还是多语言？是否有现成的钉钉应用底座？

---

## 六、红色技能降级话术（P2，被问到才用）

| 被问到 | 回答 |
|--------|------|
| Seata / 分布式事务 | “我学过 AT 模式的前后镜像和自动回滚原理，但没有在生产深度用过。我真正做过的一致性保障是大宅门的 5 分钟主动对账 + 微信回调幂等。” |
| Dubbo / ZooKeeper | “Dubbo 是 RPC 框架，ZK 做服务注册协调，我理解原理。我上一个项目用的是 Spring Cloud + Nacos。” |
| RocketMQ | “RocketMQ 特点是延迟消息、顺序消息、分区有序，我了解这些特性。真实使用中我用的是 RabbitMQ 和 Kafka。” |
| QPS 3000+ / ELK | “QPS 3000+ 是我基于 Caffeine + Redis 多级缓存做的理论估算，没有在生产环境压测。ELK 我学过原理，没有深度用过。” |
| Spring Cloud Gateway / Nacos | “我学习和实践过 Gateway 过滤器链和 Nacos 服务注册发现，了解核心概念，但没有大规模生产运维经验。” |

**核心策略**：不主动提，被问不回避；先讲概念，立马拉回自己做过的事。

---

## 七、综合问题（P2-P3）

### 7.1 离职原因（P2）
> “前一份工作让我对业务运营有了很深的理解，但我更想回到技术方向，尤其是 AI Agent。现在的工作偏销售和运营，技术成长空间有限。我希望找到一家能长期做技术、有 AI 落地场景的公司。”

### 7.2 通用反问（P2）
1. 你们团队目前 Agent 落地到什么阶段？主要在哪些业务场景？
2. 如果我来，主要负责哪块？是新的 Agent 应用开发还是现有系统的 AI 化改造？
3. 团队的技术栈偏 Python 还是 Java？对 Agent 框架选型有什么倾向？

### 7.3 不会的问题怎么办？（P3）
> “这个问题我没有在生产环境深度实践过，但我可以从原理和相似经验来回答……”  
> 然后拉回你做过的项目。  
> **金句：遇到不会的问题，诚实 + 类比 + 拉回项目，比硬编更安全。**

---

# 附录：稳定性 + 项目真实性 + 职业方向问题补充

## 1. 稳定性

### 1.1 为什么从博士眼镜离职？
> “在博士眼镜这两年多，我完全跑通了零售全链路，从销售接单、定制加工、质检到库存管理和门店交付。这段经历让我对业务流程有了很具象的理解，也让我意识到自己更想回到技术岗位。
>
> 离开的原因很简单：我想从运营分析转到后端开发，特别是 AI Agent 方向。博士眼镜的岗位性质偏运营和流程优化，技术成长空间有限。所以我离职后专心做 Python 后端和 AI Agent 实践，也是现在大宅门项目的基础。”

**金句：**
> “博士眼镜给了我业务视角，现在我想用技术能力回到后端岗位上。”


### 1.3 未来 3 年职业规划是什么？
> “第一年，深耕 AI Agent 后端工程，在一线项目里做 ReAct、RAG、MCP、工具系统和工作流编排，成为能独立负责模块的工程师。
>
> 第二年，往架构和解决方案走，能从业务需求出发设计一套可落地的 AI 方案，关注延迟、幻觉、成本、可维护性四个维度。
>
> 第三年，希望能带小团队或者成为某个垂直方向的技术负责人，把 AI 能力与行业业务结合做成标准产品。
>
> 整体方向是 AI 应用工程化 + 行业解决方案。”

**金句：**
> “短期做深技术，中期做方案，长期带团队。”

---

## 2. 技术深度

### 2.1 “三层守卫权限体系”具体是什么？
> “三层守卫是我在大宅门 v2 里用 FastAPI 依赖注入实现的权限体系。
>
> 第一层 **RequirePermission**。宣告式 RBAC，比如 `Depends(RequirePermission('store_manager', 'finance'))`，维护一个 frozenset 角色白名单，不在白名单的直接 403。
>
> 第二层 **RequireCertified**。资质认证层，比如设计师接单前必须通过 `certificationStatus == 'approved'` 的资质审核，否则 403。
>
> 第三层 **RequireIdentityVerified**。实名认证层，查 IdentityVerification 表中有 FaceID 或手机号的 verified 记录，主要用在提现前防身份风险。
>
> 三层之间是依赖注入的链式调用：先角色鉴权，再资质认证，最后实名认证。每个接口可以组合使用，比如提现接口同时加上 RequirePermission 和 RequireIdentityVerified，而查询接口只需要 RequirePermission。
>
> 这三层是功能权限鉴权，数据级别离是通过 CompanyScope 做的：分公司用户只看本公司数据，总公司自动聚合全部分公司数据。”

**金句：**
> “三层守卫的核心是依赖注入链，不是散落在每个接口里的 if-else。”

### 2.2 RAG 的召回率和准确率怎么评估？
> “PaiSmart 里我把 RAG 评估分成检索层和生成层。
>
> 检索层看召回率（Recall）。准备一个带标准答案的测试集，每个问题知道应该命中哪几个 chunk。检索后检查 top-k 里是否包含这些正确 chunk，计算 Recall@k = 正确命中的 chunk 数 / 应该命中的 chunk 总数。
>
> 生成层看准确率（Accuracy）。用同一个测试集，让系统生成答案，人工或 LLM-as-Judge 自动判断答案是否正确（正确 / 部分正确 / 错误）。
>
> 关键中间指标还有：检索的平均分数、前 top-k 里正确 chunk 占比、生成答案的幻觉率。
>
> 实际做法：先做 100 个问题左右的测试集，分别测不同 chunk 策略、不同 top-k、有无重排序的效果，再上线。”

**金句：**
> “RAG 评估分检索和生成两层：召回率看能不能找到证据，准确率看答案是不是基于证据。”

**⚠️ 备注：简历里 PaiSmart 只写了架构和流程，没写具体评估数值。被追问时可以说“这是我在学习和设计时的方法，上线后会补充实际测试”，或者补充真实数据。**

### 2.3 MCP 的 3 个 Tool 分别是什么？怎么部署？
> “PaiCLI 里的 MCP 不是只有 3 个固定工具，而是一种接入外部工具服务的协议。我接入的几类典型工具包括：
>
> 1. **文件系统类 MCP Server**：提供 read_file、write_file、list_directory 等工具，让 Agent 操作本地代码和配置。
> 2. **数据库类 MCP Server**：比如连接 PostgreSQL 的 MCP Server，提供 query 工具让 Agent 查数据库。
> 3. **网络搜索类 MCP Server**：比如提供 web_search 工具，让 Agent 获取实时信息。
>
> 部署方式分两种：
> - **stdio 模式**适合本地工具进程。配置文件里写上 `command`，比如 `python -m mcp_server_filesystem`，PaiCLI 启动时通过子进程启动它，通过 stdin/stdout 通信。这种方式轻量，不需要单独部署服务。
> - **Streamable HTTP 模式**适合远程服务或共享工具。需要单独运行一个 HTTP Server，PaiCLI 配置它的 url，通过 HTTP SSE 通信。这种方式适合多个 Agent 共享同一个工具服务。
>
> 无论哪种方式，PaiCLI 启动时都会通过 `McpClientManager` 连接这些 Server，拉取它们的 tools 列表，转成内部工具格式注册到 ToolRegistry，运行时和内置工具一起传给 LLM。”

**金句：**
> “MCP 的部署关键是选择合适的传输模式：本地轻量工具用 stdio，共享服务用 HTTP。”

---

## 4. 职业方向

### 4.1 你更倾向 Python 还是 Java？
> “我当前更倾向 Python。原因三个：
> 1. Python 在 AI Agent 和 LLM 应用开发里更高效，工具链和社区更成熟；
> 2. 我的三个主要项目里，PaiCLI-Python、大宅门 AI 直查、PaiSmart 都是 Python 主导；
> 3. 这个岗位本身也偏 Python。
>
> 但 Java 我也能做，PaiAgent 就是 Spring Boot + LangGraph4j 的 Java 项目。我觉得对于我现在的阶段，用 Python 做 AI 应用更有优势，Java 做底座和微服务保底。”

**金句：**
> “Python 是我的主攻方向，Java 是我的底座能力，不排斥。”

### 4.2 想深耕行业 SaaS 还是 AI 应用？
> “我更想深耕 AI 应用，但希望是在行业场景里做 AI 应用。
>
> 因为我有两年零售运营经验，知道业务系统的痛点在哪。单纯做行业 SaaS 可能缺少技术挑战性；单纯做 AI 应用可能缺少业务落地场景。我最想做的是把 AI Agent、RAG、工作流编排这些技术落地到具体行业的客户场景里。
>
> 比如这个钉钉 AI 岗位，就很符合我的方向：把大模型和 Agent 能力落地到企业协同、智能问答、流程自动化这些场景中。”

**金句：**
> “我不做纯行业 SaaS，也不做纯 AI 应用——我想做‘行业场景 + AI 能力’的落地。”

### 4.3 接不接受全栈？
> “我主要定位后端开发，但不排斥在需要的时候接触前端或者产品工作。
>
> 原因是我觉得一个能落地的 AI 应用，开发者需要理解业务、后端、接口和交互。我在之前的项目里也做过前后端对接、接口设计、业务需求拆解的工作。
>
> 但我的核心竞争力还是后端和 AI 工程化。如果全栈意味着每天大量写前端页面，可能会分散我在后端和 AI 方向的深度。所以我希望以后端 + AI 为主，全栈能力作为扩展。”

**金句：**
> “能接触全栈，但希望核心在后端 + AI，不想被前端页面开发占满时间。”

---

### 可直接背诵的金句
- 稳定性：“博士眼镜给了我业务视角，现在我想用技术能力回到后端岗位上。”
- 职业规划：“短期做深技术，中期做方案，长期带团队。”
- 三层守卫：“三层守卫的核心是依赖注入链，不是散落在每个接口里的 if-else。”
- RAG 评估：“RAG 评估分检索层和生成层，召回率看能不能找到证据，准确率看答案是不是基于证据。”
- MCP 部署：“本地轻量工具用 stdio，共享服务用 HTTP。”
- Python vs Java：“Python 是主攻方向，Java 是底座能力，不排斥。”
- 行业 SaaS vs AI：“我想做‘行业场景 + AI 能力’的落地。”
- 全栈：“能接触全栈，但希望核心在后端 + AI，不想被前端页面开发占满时间。”
