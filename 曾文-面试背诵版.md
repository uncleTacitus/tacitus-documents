# 曾文 - AI Agent 面试背诵版

> 最终版：2026-07-28  
> 使用方法：按顺序背，每段控制在标注时间内，背熟后录音自检。

---

## 1. 自我介绍（2 分钟，必背）

> 面试官你好，我叫曾文。
>
> 我有两条线——一条技术线，一条业务线。
>
> 技术上，我是 Python + Java 双栈后端。Python 端用 FastAPI + SQLAlchemy 2.0 异步栈做过真实上线的家装分销系统，涉及多角色权限、线索状态机、微信支付资金一致性。Java 端我系统学习并实践过 Spring 微服务、消息队列、分布式事务等原理。
>
> 最近一年我重点投入 AI Agent 方向。我系统学习了 PaiAgent（工作流编排平台）和 PaiCLI-Python（终端 Agent CLI）的核心实现，深入理解了 ReAct Loop、Tool Registry、MCP、RAG、记忆系统、多 LLM 提供商集成等 Agent 生产化技术。
>
> 同时在我负责的公司项目里，我也引入了 AI 能力——为企微机器人设计了一个 Data Chart Router 层，对中文查询做意图匹配，直读数据库生成图表返回，绕过 LLM 降低了延迟和幻觉。
>
> 业务线上，我在博士眼镜做过两年零售运营，从销售到供应链到库存都跑过一遍。这段经历让我对“流程”有非常具象的理解——做 Agent 工作流引擎的时候，我比纯技术出身的同学更清楚工作流应该怎么设计、边界条件在哪、异常怎么处理。
>
> 我的优势就是把后端的工程落地能力 + AI Agent 的核心原理理解 + 业务的流程思维结合在一起。我不是跟算法岗拼论文，我跟工程岗拼 Agent 系统的落地能力。
>
> 谢谢。

---

## 2. PaiAgent 双引擎 60 秒

> "PaiAgent 双引擎通过 EngineSelector 路由，默认 DAG。DAG 拓扑排序、无环、线性执行，简单好调试；LangGraph 用状态机 + 条件边处理循环和复杂决策。两个引擎共享 NodeExecutor 接口，LangGraph 通过 NodeAdapter 适配执行，所以加新节点类型两个引擎自动能用。"

**金句**："不是两套实现，是共享节点执行器，只有调度层不同。"

---

## 3. Bug STAR 6545e5故事 90 秒

> "我在本地跑 PaiAgent 的 ReAct 工作流，验证记忆写入功能。输入是‘记住我叫张三，问我今天星期几’，运行后发现 Agent 反复尝试调用 memory_write，每次失败都报一个很模糊的 invoke 错误，DebugDrawer 看不到真实原因。
>
> 我顺着堆栈定位到 VolcengineAgentPlanClient.java。原代码在 HTTP 失败时直接调用 conn.getErrorStream().readAllBytes()，但 getErrorStream() 可能返回 null，一定是 NPE，把真实的 embedding 错误盖住了。
>
> 我改成先判空：InputStream stream = ...; byte[] bytes = stream != null ? stream.readAllBytes() : new byte[0];；同时处理 text.isBlank() 的情况，避免空错误信息。
>
> 修完之后再次运行，直接看到了 memory_write 的真实错误——embedding 接口配置缺失。这个改动虽然就两三行，但把后续所有 Agent Plan 工具调用的排查成本从盲猜变成看真实的 HTTP 状态码和错误响应，是一个小但关键的防御性改进。"

---

## 4. PaiAgent 工具注册与发现 90 秒

> "PaiAgent 工具分三层。
>
> 第一层注册：启动时 AgentToolRegistry 扫描 Component 注册内置工具（如 memory_write），同时从 mcp_tool_config 表加载配置好的 MCP 工具，全量存在 registry 里。
>
> 第二层调用：ReActAgentNodeExecutor 拿到节点配置的工具名列表，去 registry 查出 AgentTool 实例，把它的 name、description、inputSchema 拼进 LLM 调用的 tools 参数。LLM 返回 tool_calls 时，executor 按名从已查好的实例池里取出来执行。
>
> 第三层展示：问题在前端 EditorPage.tsx 硬编码了 memory_write 等几个工具，没调用后端接口动态拉取全部工具。所以用户配置了 MCP Server，工作流里也选不到。后端其实已经支持扩展了，限制在前端展示层。"

**金句**："后端已经支持 MCP 扩展，限制主要在前端展示层。"

---

## 5. PaiAgent ReAct 运行时 60 秒

> "PaiAgent 的 ReAct Agent 在 ReActAgentNodeExecutor 里是一个 for 循环，默认最多 5 步，上限 20 步。
>
> 每步 executor 把从 AgentToolRegistry 查出来的工具 schema 拼进 LLM 调用的 tools 参数。LLM 返回一个 JSON 决策，executor 用 JSONObject.parseObject() 解析，提取 action 和 action_input。
>
> 然后判断 finish_reason：如果是 stop 就给最终答案结束循环；如果是 tool_calls 就去 registry 查工具实例执行，结果塞回 messages，继续循环。步数到了强制终止。"

**金句**："ReAct 的智能来自循环 + 工具调用失败反馈，Agent 能根据反馈调整，直到找到答案或达上限。"

---

## 6. PaiCLI ReAct Loop 实现 60 秒

> "PaiCLI-Python 的 ReAct Loop 实现和 PaiAgent 思路一致：一个 while 循环，每轮调用 OpenAI-compatible LLM，把 tools schema 放进 `tools` 参数。
>
> LLM 返回的 response 里如有 tool_calls，执行对应工具函数，把结果按 OpenAI 格式组装成 role=‘tool’ 的 message，追加回 messages。
>
> 直到 LLM 不再返回 tool_calls，返回最终答案。同时还有 max_steps 上限、事件流式输出等增强。"

---

## 7. PaiCLI Tool Registry 60 秒

> "PaiCLI 的工具是 Python 装饰器模式。用户把工具函数用 `@tool` 装饰，系统自动从函数签名和 docstring 生成 JSON Schema。
>
> 开始任务时，AgentRunner 把所有工具注册进去，每次调用 LLM 时传入 tools 参数。LLM 根据 name 和 description 决定调哪个。有新工具时，继承一个基类并注册即可。"

---

## 8. PaiCLI MCP Client 60 秒

> "PaiCLI 的 MCP Client 支持 stdio 和 Streamable HTTP 两种传输方式。stdio 适合本地工具进程，比如文件系统、数据库工具；HTTP 适合远程服务，比如谷歌搜索、企业内部 API。
>
> 平时我们连接 MCP Server 后，把它提供的 tools 列表转成内部工具格式，一起传给 LLM。此外 PaiCLI 自身也可以作为 MCP Server 暴露内置工具。"

---

## 9. PaiCLI Plan-and-Execute 60 秒

> "Plan-and-Execute 和 ReAct 的区别是：ReAct 是‘边想边做’，每步根据上一步结果决定下一步；Plan-and-Execute 是‘先计划再执行’。
>
> PaiCLI 里的流程是：Planner 接到用户请求后，生成一个 DAG 任务图；然后 Worker 们按依赖关系并行执行；每个 Worker 完成后 Reviewer 检查结果，不通过则重试或终止。这种方式适合步骤明确、需要强可控性的复杂任务。"

---

## 10. PaiCLI 记忆系统 60 秒

> "PaiCLI 的记忆分三层。
>
> 短期记忆管理当前对话的 messages，保持上下文连贯。长期记忆把关键事实存到 SQLite，跨会话复用。每次 Agent Run 前后自动打快照，如果执行中断了可以恢复现场。
>
> 这样一来得到两个好处：一是 context 快满时可以用长期记忆和快照减轻压力；二是重要信息不会每次重新渲染，比较高效稳定。"

---

## 11. RAG 全流程 + Chunk 策略 90 秒

> "RAG 全流程是这样的：
>
> 1. 文档解析：把 PDF、Word 等原始文件拆成纯文本，比如用 Apache Tika；
> 2. 分块：按一定策略切成 Chunk。固定大小最简单；按段落更符合语义；按主题分割适合知识密集场景；代码则按文件/类/方法粒度，并做 AST 解析。
> 3. Embedding：把每个 Chunk 打成向量。可以走远程 API，也可以本地 Ollama。
> 4. 索引：存进向量数据库或查询引擎，比如 Elasticsearch、SQLite+vec、PostgreSQL pgvector。
> 5. 检索：用户问题时也打成向量，找最相近的 K 个 Chunk。
> 6. 生成：把检索结果连同问题一起传给 LLM，生成带引用的回答。
>
> Chunk 策略的 trade-off 是：太小上下文不足，太大精度不够。我们一般先按语义边界划分，再在边界内给一个上限。对于代码，一定要保持方法粒度完整，不能在方法中间切开。"

---

## 12. MCP vs Function Calling 90 秒

> "Function Calling 是 LLM 的一种能力：模型看到几个函数定义，可以输出调用参数，由应用层直接执行本地函数。
>
> MCP 是一个标准协议，定义了 LLM 应用怎么连接外部工具服务。它把工具做成独立进程或服务，比如文件系统 MCP Server、PostgreSQL MCP Server。
>
> 核心区别三点：1）Function Calling 是调用能力，MCP 是连接协议；2）Function Calling 的函数在应用内部，MCP 的 Server 是独立进程；3）MCP 的工具可以被多个 Agent 复用，Function Calling 的函数通常只服务当前应用。
>
> MCP 的代价是要单独部署 Server，Function Calling 更轻量。"

**金句**："Function Calling 是 LLM 怎么调函数，MCP 是怎么让 LLM 安全地调用外部系统的工具。"

---

## 13. 大宅门推客系统 项目介绍 60 秒

> "大宅门推客系统 v2 是一个家装行业的分销与线索运营平台，后端基于 FastAPI + SQLAlchemy 2.0 异步栈，数据库用 PostgreSQL 16 + Redis 7。
>
> 我负责后端架构与核心开发，主要覆盖四块：多角色权限与数据隔离、线索全生命周期状态机、多级佣金与微信支付对账、企微机器人的 AI 数据图表直查。
>
> 系统 142+路由，我们从 v1 数据库结构探为主线索重构并完成了生产迁移，主要是为了支持多角色、等级化佣金与更安全的支付流程。"

---

## 14. 大宅门线索状态机 60 秒

> "v1 的线索状态是单值字段，不能同时表达多种业务属性，比如是否已报价、是否已上门、是否成交，导致后续奖励触发很难做出确定性判断。
>
> v2 我们把状态改成 11 位状态标记，每一位代表一个业务状态。同时独立设奖励关联位。这样好处是奖励触发只需位运算，天然幂等：同一个位只能从 0 变 1，无论主管恢复或者系统重试，不会重复发放奖励。
>
> 此外查询时也可以位运算一次过滤多种状态，性能比存多个 boolean 字段好。"

---

## 15. 大宅门权限三层守卫 60 秒

> "v1 的权限是硬编码的，不好维护。v2 我们做了三层守卫：
>
> 1. RequirePermission：基于角色鉴权，判断用户是否有某个功能权限；
> 2. RequireCertified：认证级判断，比如是否完成了实名认证或者经销商认证；
> 3. RequireIdentityVerified：身份验证，比如微信授权后才能查看某些敏感数据。
>
> 同时做了 CompanyScope 数据隔离：普通员工只能看自己公司，分公司管理员能看自己分公司及下级，总公司自动聚合所有分公司数据。这些守卫与过滤器都写成装饰器，在路由层统一应用。"

---

## 16. 大宅门微信支付佣金对账 60 秒

> "佣金流程是这样的：线索成交后，系统按级别给子员工、经理、总经等多个角色计算佣金，形成一条多级佣金链路。
>
> 提现方面，<=400 元直接通过微信企业转账到帐；>400 元需要主管审核后才能打款。
>
> 为了保证支付状态与我们库里一致，我们每 5 分钟跑一次定时任务，主动对账提现记录与微信支付结果。如果发现不一致，按微信结果准标记，库里状态做修正。这样就不会出现重复打款或者库里已成功但实际失败的情况。"

---

## 17. AI 数据图表直查 90 秒

> "我在公司项目里做了一个 Data Chart Router，思路是‘规则前置，LLM 兜底’。
>
> 第一层是规则命中：用户问‘本月销售额’‘今日新增线索’这类高频中文查询，我用正则做意图匹配，命中后直接查 PostgreSQL 生成 SVG/PNG 返回，完全不调用 LLM。这样响应快、无幻觉。
>
> 第二层是规则未命中但听起来像查数据：给用户友好回复‘这个查询暂时不支持，已记录’，后台异步补规则。
>
> 第三层是非数据类闲聊：比如‘早上好’，走 OpenClaw Gateway 的 LLM SSE 流式回复。
>
> Trade-off 是开发侧要维护规则表和 SQL，但换来了低延迟和高确定性。同时我们对不同指标做了分级缓存，图表标注上次更新时间，让用户自己判断数据是否新鲜。"

**金句**："生产级 AI 系统的核心不是让 LLM 做所有事，而是让 LLM 只做它该做的事。"

---

## 18. 非科班为什么能做 Agent 后端 60 秒

> "Agent 后端不是算法岗，重点是工程落地能力。我的优势有三点：
>
> 1. 双栈工程能力：Python FastAPI 异步栈有上线生产项目（大宅门），Java Spring 体系有学习和实践经验；
> 2. AI 工程实践：系统学习过 PaiAgent、PaiCLI-Python 等开源 Agent 项目，完整覆盖了 ReAct、Multi-Agent、MCP、RAG、Memory 等 Agent 生产化技术栈；
> 3. 业务视角：两年零售运营经验，做过销售、供应链、库存的全链路流程优化。这种流程思维对设计 Agent 工作流特别重要——我知道边界条件在哪、异常怎么处理。
>
> 所以我不跟算法岗拼论文，我跟工程岗拼 Agent 系统的落地能力。"

---

## 19. 离职原因 30 秒

> "前一份工作让我对业务运营有了很深的理解，但我更想回到技术方向，尤其是 AI Agent。现在的工作偏销售和运营，技术成长空间有限。我希望找到一家能长期做技术、有 AI 落地场景的公司。"

---

## 20. 反问问题（准备 3 个）

1. 你们团队目前 Agent 落地到什么阶段？主要在哪些业务场景？
2. 如果我来，我主要负责哪块？是新的 Agent 应用开发还是现有系统的 AI 化改造？
3. 团队的技术栈偏 Python 还是 Java？对 Agent 框架选型有什么倾向？

---

## 21. 红色技能降级话术（被问到就这么说）

| 被问到 | 话术 |
|--------|--------|
| Seata / 分布式事务 | "这个我学过原理，了解 AT 模式的前后镜像和自动回滚，但没有在生产环境深度用过。我目前主要做的业务一致性方案是大宅门里的 5 分钟主动对账。" |
| Dubbo / ZooKeeper | "Dubbo 是 RPC 框架 + ZooKeeper 做服务注册与协调，我理解原理。我实际项目中用的是 Spring Cloud + Nacos。" |
| RocketMQ | "RocketMQ 我看过文档了解特性，比如延迟消息、顺序消息。真实使用中我用的是 RabbitMQ 和 Kafka。" |
| QPS 3000+ / ELK | "QPS 3000+ 是我对多级缓存方案的理论估算（Caffeine+Redis），没有在生产环境真实压测。ELK 我学过原理，没有深度用过。" |

---

## 背诵顺序建议

**第一遍**（今晚）：1 自我介绍 → 2 双引擎 → 3 Bug → 17 AI 数据直查
**第二遍**（明早）：13-16 大宅门 → 4-5 工具/ReAct → 11-12 通用
**第三遍**（明中）：6-10 PaiCLI → 18 非科班 → 19 离职 → 20 反问
**面试前 1 小时：**1 自我介绍 + 2-3 个 STAR 故事即可
