# 曾文 - 今晚救命版（最终简化背诵）

> 适用场景：只有 3 小时，不能完整复述答题，但必须能口述出每个项目的价值。
> 核心策略：不背细节，只背**67 个核心故事** 和 **1 个万能回答框架**。

---

## 00. 万能回答框架（三步法，所有技术题都用它）

被问任何技术问题时，按这个顺序说：

**1. 一句话定位（给出答案边界）**
> 例："RAG 的核心思路是先检索知识、再拼进上下文让 LLM 生成。"

**2. 我在哪个项目里做了类似的事（拉回简历）**
> 例："在我做企微机器人的数据图表直查里，它就是一个轻量版 RAG。"

**3. 三个关键设计点（带结构说）**
> 例：
> - "第一，用意图匹配决定是不是数据查询；"
> - "第二，查到后用预置 SQL 直读数据库；"
> - "第三，非数据类问题才走 LLM 。"

**4. 一句金句收尾（让面试官记住你）**
> 例："Agent 系统的核心不是让 LLM 做所有事，而是让 LLM 只做它该做的事。"

---

## 0. 自我介绍（必背，2 分钟内）

说三个线索：

> 面试官你好，我叫曾文。
>
> 我有两条线：技术线 + 业务线。
>
> 技术线：我是 Python + Java 双栈后端。Python 端用 FastAPI + SQLAlchemy 2.0 异步栈做过真实上线的家装分销系统，涉及多角色权限、线索状态机、微信支付资金一致性。Java 端我系统学习并实践过 Spring 微服务、消息队列、分布式事务等原理。
>
> 近一年重点学习 AI Agent 方向，深入理解了 PaiAgent、PaiCLI-Python 的核心实现，涉及 ReAct、Tool Registry、MCP、RAG、记忆系统、多 LLM 提供商集成。
>
> 业务线：我在博士眼镜做过两年零售运营，从销售到供应链到库存都跑过一遍。这让我对“流程”有很具象的理解——做 Agent 工作流引擎的时候，我比纯技术出身的同学更清楚工作流应该怎么设计、边界条件在哪、异常怎么处理。
>
> 我的优势就是把**后端的工程落地能力 + AI Agent 的核心原理理解 + 业务的流程思维**结合在一起。我不跟算法岗拼论文，我跟工程岗拼 Agent 系统的落地能力。
>
> 谢谢。

---

## 1. 故事一：大宅门推客系统（用于回答“你做过最复杂的系统”“你怎么处理业务复杂度”）

### 一句话定位
家装行业线索分销与运营平台，从 v1 Django 重构到 v2 FastAPI 异步栈，核心解决多角色权限、状态机、资金安全三个问题。

### 4 句结构号（必背）
> 1. "多角色权限：v1 每个接口散写 if-else，v2 收拂成 RequirePermission + RequireCertified + RequireIdentityVerified 三层装饰器，加 CompanyScope 数据隔离，总公司自动聚合所有分公司数据。"
>
> 2. "状态机：v1 状态是单值字段，次模糊。v2 改成 17 位 JSONB 状态标记，奖励触发是独立位，主管恢复线索也不会重复发奖。"
>
> 3. "微信支付佣金对账：先 commit 再调微信，回调 + 状态 + 金额三层幂等，每 5 分钟主动对账轮询，不一致时按微信结果为准，库里状态修正。"
>
> 4. "我负责架构设计和 v1→v2 生产迁移，17 步 Alembic 脚本 + 数据校验，完成无回滚迁移。"

### 金句
> "业务复杂系统最容易出事的不是功能，是状态和资金流的一致性。我们用状态位 + 幂等保障 + 主动对账这三件事把这两块管住了。"

---

## 2. 故事二：AI 数据图表直查（用于回答“AI 项目”“你怎么用 LLM”“RAG”“意图识别”）

### 一句话定位
给企微机器人做的一个 Data Chart Router，规则前置，LLM 兜底，高频数据查询绕过 LLM 直读数据库。

### 4 句结构号
> 1. "第一层规则命中：高频中文查询用正则做意图匹配，命中直接查 PostgreSQL 生成 SVG/PNG。"
>
> 2. "第二层数据类未命中：给友好提示'暂时不支持，已记录'，后台异步补规则。"
>
> 3. "第三层闲聊走 LLM：比如'早上好'，通过 OpenClaw Gateway 走 SSE 流式回复。"
>
> 4. "分级 TTL 缓存 + 图表标注上次更新时间，裤子浮空；这是幻觉风险的常用做法。"

### 金句
> "生产级 AI 系统的核心不是让 LLM 做所有事，而是让 LLM 只做它该做的事。"

---

## 3. 故事三：PaiAgent AI 工作流平台（用于回答“Agent”“ReAct”“多引擎”“多 LLM”“你解决的 Bug”）

### 一句话定位
PaiAgent 是一个可视化的 AI Agent 工作流编排平台，支持 DAG + LangGraph 双引擎，ReAct Agent 节点，Tool Registry，Skill 系统，多 LLM provider。

### 4 句结构号
> 1. "双引擎：DAG 线性无环适合固定流水线，LangGraph 状态图适合循环和复杂决策，两个引擎共享同一套 NodeExecutor。"
>
> 2. "ReAct 节点：一个 for 循环，每次拿工具 schema 给 LLM，LLM 返回 tool_calls 后执行，结果塞回 messages，max_steps 限制。"
>
> 3. "工具系统：启动时 AgentToolRegistry 注册内置工具 + 从 mcp_tool_config 加载 MCP 工具，节点配置哪些工具名就给 LLM。"
>
> 4. "我修过一个关键 Bug：VolcengineAgentPlanClient 在 HTTP 错误时 getErrorStream() 可能为 null，原代码引发 NPE 盖住了真实错误。加 null 判断后，直接看到是 embedding 接口配置缺失，排查成本从盲猜变成看状态码。"

### 金句
> "Agent 平台的架构重点不是跟风加功能，而是让节点类型、引擎、工具都能插拔式扩展。"

---

## 4. 故事四：PaiCLI-Python 终端 Agent（用于回答“MCP”“工具注册”“ReAct Loop”“记忆”）

### 一句话定位
PaiCLI 是一个终端 Agent CLI，用 Python 实现 ReAct Loop、Tool Registry，支持 MCP 连接外部工具、长期记忆 SQLite 和 snapshot 回滚。

### 4 句结构号
> 1. "ReAct Loop：一个 while 循环，每轮把 messages 和 tools schemas 传给 LLM，有 tool_calls 就执行，结果组装成 role='tool' 的 message 追加回去，max_steps 限制。"
>
> 2. "工具注册：装饰器模式，`@tool` 装饰函数自动生成 JSON Schema，启动时注册到 ToolRegistry。"
>
> 3. "MCP：支持 stdio 和 Streamable HTTP 两种传输，把外部工具转成内部格式给 LLM，PaiCLI 自身也能作为 MCP Server。"
>
> 4. "记忆：三层——项目级文件约定、SQLite 长期记忆、对话 history 短期记忆，每次运行前后自动快照，错了就 restore。"

### 金句
> "终端 Agent 的信任链是工具可审计、写操作可回滚、长期记忆可追溯。"

---

## 5. 通用技术题万能套路（用于“RAG”“MCP vs Function Calling”“多 Agent”“评估”）

### RAG 全流程
> 一句定位："RAG 是先检索知识、再让 LLM 生成的模式。"
>
> 三句结构：
> 1. "文档解析成纯文本，按策略切 chunk——固定大小、按段落、按主题、按代码结构。"
> 2. "每个 chunk 打 embedding，存进向量库；用户问题时也打向量去检索 top-k。"
> 3. "检索结果拼进 prompt，LLM 基于这些证据回答，降低幻觉。"
>
> 金句："RAG 不是只有向量检索，只要是'先检索知识再生成'就是 RAG，关键词、BM25 也算。"

### MCP vs Function Calling
> 一句定位："Function Calling 是 LLM 调用本地函数的能力，MCP 是连接外部工具服务的标准协议。"
>
> 三句结构：
> 1. "Function Calling 的函数在应用内部，MCP 的 Server 是独立进程/服务。"
> 2. "Function Calling 适合单应用内部工具，MCP 适合多个 Agent 共享外部能力。"
> 3. "MCP 代价是要部署 Server，Function Calling 更轻量。"
>
> 金句："Function Calling 是 LLM 怎么调函数，MCP 是怎么让 LLM 安全地调用外部系统。"

### 多 Agent 协作
> 一句定位："多 Agent 就是多个角色分工，核心是任务拆分、状态共享、冲突解决。"
>
> 三句结构：
> 1. "顺序接力：Agent A 完成传给 Agent B，像流水线。"
> 2. "主从分配：Manager 拆分配、Worker 并行执行、Reviewer 检查。"
> 3. "圆桌讨论：多角色对同一问题反复讨论迭代，比如架构师 + 测试 + 安全。"
>
> 金句："多 Agent 不是堆数量，是任务拆清楚、状态管明白、冲突有规则。"

### Agent 评估
> 一句定位："Agent 评估看的是任务是否真正完成，不是 LLM 输出是不是流畅。"
>
> 四个维度：任务完成度、工具调用正确性、输出质量、成本效率（token / 轮数 / 时延）。
>
> 金句："Agent 评估不是评 LLM，是评一个能自主调工具完成任务的系统。"

---

## 6. 红色技能被问：这样减级（不主动提，不回避问）

| 被问 | 回答 |
|------|--------|
| Seata / 分布式事务 | "我学过 AT 模式的前后镜像和自动回滚原理，但没在生产深度用过。我真正做过的一致性保障是大宅门的 5 分钟主动对账 + 微信回调幂等。" |
| Dubbo / ZK | "Dubbo 是 RPC 框架，ZK 做服务注册协调，我理解原理。我上一个项目用的是 Spring Cloud + Nacos。" |
| RocketMQ | "RocketMQ 特点是延迟消息、顺序消息、分区有序，我了解这些特性。真实使用中我用的是 RabbitMQ 和 Kafka。" |
| QPS 3000+ | "那是我基于 Caffeine + Redis 多级缓存做的理论估算，没有在生产环境压测。我真正做过的性能优化是大宅门的分级缓存和状态位查询优化。" |
| ELK | "我学过 ELK 的日志采集、索引、可视化原理，但没有深度部署过。我真正做过的日志和监控是大宅门的 Sentry + PostgreSQL 慢查询日志。" |
| Gateway / Nacos | "我学习和实践过 Gateway 过滤器链和 Nacos 服务注册，了解原理。但我没有大规模生产运维经验，主要做的是应用层开发。" |

---

## 7. 今晚 3 小时安排（必须严格执行）

### 19:30-20:30 第一轮：背熟 4 个主干
目标：自我介绍 + 3 个故事能在无备稿情况下顺利说出来。

1. 先读 3 遍自我介绍，然后合上稿子自己说 3 遍。
2. 每个故事先读 2 遍，然后自己说 3 遍，用手机录音。
3. 不求完美，求**80% 一致** + 金句不忘。

### 20:30-21:30 第二轮：随机组合练习
目标：练习把不同问题拉回到自己的故事。

随便抽 10 个简历上的点，用这个句式开头：
> "这个问题我在项目里遇到过，我是这么处理的...”

然后直接套上面的 4 句结构号，不要拒绝答，先说滑了再说。

### 21:30-22:30 第三轮：录音自检
目标：发现卡壳点，做一张明早提示卡。

1. 从第一轮录音中找 3-5 个卡壳点。
2. 把卡壳点写成“关键词”小纸条，比如：
   - 状态机 → “17 位 JSONB + 奖励位独立 + 幂等”
   - 对账 → “先 commit 再调微信 + 三层幂等 + 5 分钟轮询”
   - RAG → “解析 → chunk → embed → 检索 → 生成”
3. 明早起床直接看小纸条，不用再看大文档。

---

## 8. 明早出门前 1 小时（如果面试在周日）

1. 再说 1 遍自我介绍录音。
2. 看 1 遍小纸条上的关键词。
3. 准备 1 个故事句子候选：大宅门状态机 / AI 图表直查 / PaiAgent Bug，面试官让你“举例”时直接用。
4. 心态：面试官问的不是你有多少知识，而是你能不能在压力下把你做过的事清晰说出来。

---

## 总结

今晚必须做到：
- [ ] 自我介绍能脱稿说 1 遍
- [ ] 故事 1 大宅门 能脱稿说 1 遍
- [ ] 故事 2 AI 直查 能脱稿说 1 遍
- [ ] 故事 3 PaiAgent 能脱稿说 1 遍
- [ ] 录音自检，生成 5-10 个关键词小纸条

明早：只背小纸条，不背新内容。
