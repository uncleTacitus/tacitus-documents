# 曾文 - 今晚救命版（面试口述版）

> 适用场景：只有 3 小时，不能背大段落，只背 4 个项目故事 + 1 个万能回答套路。
> 说话风格：像给朋友解释一样说，技术词少但准确，金句一句记住。

---

## 一、万能回答套路

面试官问什么技术问题，都可以按这 4 步回答：

**1. 一句话说清楚是什么**
（让面试官知道你听懂了）

**2. 拉回自己的项目**
（让他知道你做过，不是只会背书）

**3. 讲 3 个关键点**
（用“第一、第二、第三”带节奏）

**4. 一句金句收尾**
（让面试官记住你）

### 举例：被问 RAG 是什么

> RAG 就是先查资料，再让 LLM 回答，这样不会胡编。
>
> 我在企微机器人里做过类似的事：用户问销售额，我不让 LLM 现编，而是先匹配意图，再直接查数据库。
>
> 第一，把用户问题分类，看是不是数据查询；第二，数据查询直接读数据库，不走 LLM；第三，闲聊类问题才交给 LLM。
>
> 所以我的理解是：生产级 AI 不是让 LLM 做所有事，而是让 LLM 只做它该做的事。

---

## 二、自我介绍（2 分钟内）

>面试官你好，我叫曾文。
>
>我有两条线：技术线和业务线。
>
>技术上，我是 Python + Java 双栈后端。Python 这边，我用 FastAPI + SQLAlchemy 2.0 异步栈做过一个上线的家装分销系统，里头有多角色权限、线索状态机、微信支付对账。Java 这边，我系统学习并实践过 Spring 微服务、消息队列、分布式事务等原理。
>
>最近一年我重点学习 AI Agent 方向，深入理解了 PaiAgent（工作流编排平台）和 PaiCLI-Python（终端 Agent）的核心实现，涉及 ReAct 循环、工具注册、MCP、RAG、记忆系统、多 LLM 提供商集成。
>
>业务上，我在博士眼镜做过两年零售运营，从销售到供应链到库存都跑过。这段经历让我对“流程”有很具象的理解，做 Agent 工作流引擎时，我比纯技术出身的同学更清楚边界在哪、异常怎么处理。
>
>我的优势就是三点结合：后端工程落地能力 + AI Agent 核心原理理解 + 业务流程思维。我不跟算法岗拼论文，我跟工程岗拼 Agent 系统的落地能力。
>
>谢谢。

---

## 三、项目 1：大宅门推客系统

### 这是个什么项目？
一个家装行业的分销系统。客户被推荐官拉进来以后，经过店小二、设计师、定金、签约、开工几个环节，每个环节都有不同角色处理，也都涉及佣金和奖励。

### 我做了什么？
我负责从 v1 重构到 v2，把 Django 同步框架改成 FastAPI 异步框架，核心解决三个问题：

- **多角色权限**：v1 权限散在几十个接口里写 if-else，v2 收敛成三层装饰器：角色鉴权、资质鉴权、实名鉴权。
  - 关键词：RequirePermission、RequireCertified、RequireIdentityVerified、CompanyScope

- **线索状态机**：v1 状态是一个字段字符串，不能同时表达“已分配”“已进店”“已签约”。v2 改成 17 个 JSONB 状态位，每个业务状态独立。
  - 关键词：17 位 JSONB、奖励位独立、幂等
  - 好处：主管恢复线索后再签约，系统不会重复发奖励

- **微信支付佣金对账**：涉及多级佣金、小额直接打款、大额主管审核、每 5 分钟主动对账。
  - 关键词：先 commit 再调微信、回调幂等、状态幂等、金额一致性、定时轮询

### 这个项目能应对什么面试问题？
- 你做过最复杂的系统是什么？
- 怎么设计权限？
- 状态机怎么做？
- 支付安全怎么保障？
- 分布式事务不熟怎么办？（拉回对账）

### 金句
**“业务系统最容易出问题的不是功能，是状态和资金流的一致性。我们用状态位 + 幂等保障 + 主动对账这三件事把两个风险点管住了。”**

---

## 四、项目 2：AI 数据图表直查

### 这是个什么项目？
给企业微信机器人做的一个数据查询层。员工在企微群里问“本月销售额”“今日新增多少线索”，机器人直接返回图表。

### 核心思路：规则前置，LLM 兜底

第一层：“月销售额”“今日新增”这类常问的数据查询，用正则匹配意图，直接查 PostgreSQL 生成图表。这里不调用 LLM，所以快且准。

第二层：也像查数据但没被规则覆盖到，比如某个新的综合指标。这时候不胡编回复，而是说“这个查询暂时不支持，已记录”，后台异步补规则。

第三层：闲聊问题比如“早上好”，才走 LLM 流式回复。

### 为什么要绕过 LLM？
因为数据查询要求：
- 快：给一张图不能等几十秒；
- 准：销售额不能编错；
- 可控：不能让 LLM 现写 SQL，防止注入和越权。

### 关键技术词
意图匹配、正则表、预置 SQL、分级 TTL 缓存、SSE 流式输出、数据幻觉

### 这个项目能应对什么面试问题？
- 你做过什么 AI 项目？
- LLM 怎么落地？
- 怎么减少幻觉？
- RAG 你怎么做的？
- 为什么不直接让 LLM 查数据库？

### 金句
**“生产级 AI 不是让 LLM 做所有事，而是让 LLM 只做它该做的事。”**

---

## 五、项目 3：PaiAgent 工作流平台

### 这是个什么项目？
一个可拖拽节点的 AI Agent 工作流编排平台。用户可以在画布上拖几个节点，组成一个工作流，例如“接到用户问题 → 调用搜索工具 → 调用 LLM 回复”。

### 我做了什么？

- **双引擎**
  - DAG 引擎：节点无环、线性执行，适合固定流水线。
  - LangGraph 引擎：带状态机和条件边，适合循环和复杂决策，比如“检查 → 判断 → 可能重试”。
  - 两个引擎共享同一套节点执行器，加新节点两边都能用。

- **ReAct Agent 节点**
  - LLM 每轮决定是不是要调工具；
  - 如果调，执行后把结果再塞回对话；
  - 最多 20 步强制结束，防止死循环。

- **工具注册与发现**
  - 内置工具比如记忆写入、搜索；
  - 支持 MCP 连接外部工具服务；
  - 节点配置哪些工具名，LLM 就只能看到哪些工具描述。

- **我修过的一个 Bug**
  - 火山引擎的 HTTP 错误处理里，原代码直接读 error stream，但 error stream 有时候是 null，就会 NPE，把真实错误盖住。
  - 我加了 null 判断，排查问题从“盲猜”变成“看 HTTP 状态码”，后续所有 Agent 工具调用都受益。

### 关键技术词
DAG、LangGraph、ReAct Loop、NodeExecutor、Tool Registry、MCP、Skill 系统、多 LLM provider、ChatClientFactory、防御性编程

### 这个项目能应对什么面试问题？
- 什么是 Agent？
- ReAct 循环是怎么回事？
- DAG 和 LangGraph 的区别？
- 工具怎么给 LLM 用？
- MCP 是什么？
- 你解决过什么复杂 Bug？

### 金句
**“一个好的 Agent 平台，不是功能多，而是节点、引擎、工具都能插拔式扩展。”**

---

## 六、项目 4：PaiCLI-Python 终端 Agent

### 这是个什么项目？
一个给程序员用的终端 Agent CLI。你告诉它“给这个项目加一个用户登录功能”，它会自己读代码、写代码、跑测试、生成提交信息。

### 我做了什么？

- **ReAct Loop**
  - LLM 每次决定调不调工具；
  - 调完工具把结果告诉 LLM；
  - 一直循环到任务完成。

- **工具注册**
  - 用 `@tool` 装饰器注册函数；
  - 自动从函数签名和文档生成 JSON Schema；
  - LLM 看到 schema 后决定调哪个工具。

- **MCP 连接**
  - 支持本地进程（stdio）和远程服务（HTTP）两种方式；
  - 可以连接外部工具，比如文件系统、数据库、搜索引擎；
  - PaiCLI 自己也能当外部工具服务用。

- **记忆和快照**
  - 短期记忆：当前对话历史；
  - 长期记忆：存 SQLite，跨会话记住用户偏好；
  - 快照：每次运行前后自动备份代码，改坏了可以 restore。

### 关键技术词
ReAct Loop、Tool Registry、MCP stdio/HTTP、SQLite 记忆、snapshot 回滚、HITL 审计

### 这个项目能应对什么面试问题？
- 什么是 MCP？
- 工具怎么注册给 LLM？
- Function Calling 和 MCP 有什么区别？
- 终端 Agent 的记忆怎么做？
- 写操作怎么保证安全？

### 金句
**“终端 Agent 的信任链：工具可审计、写操作可回滚、长期记忆可追溯。”**

---

## 七、通用技术题（用日常语言解释）

### RAG 的完整流程

RAG 就是“先查资料，再让 LLM 回答”，避免胡编。

流程：
1. 文档解析：把 PDF/Word 等转成纯文本；
2. 切块：把大文档切成小块，比如按段落、按主题、按代码函数；
3. 打向量：每个块变成一个向量数字；
4. 存库：存到向量数据库或查询引擎；
5. 检索：用户问题也打成向量，找最相近的几个块；
6. 生成：把找到的块拼进 prompt，LLM 基于这些资料回答。

**金句**：**RAG 不一定非得是向量，只要是“先查资料再回答”就叫 RAG，关键词、BM25 也算。**

### MCP vs Function Calling

**Function Calling**：LLM 的一项能力，能根据函数描述输出调用参数，但函数还是应用里自己写的。

**MCP**：一个标准协议，让外部服务器也能提供工具给 LLM。比如一个文件系统 MCP Server，多个 Agent 都能连。

**区别**：
- Function Calling 是调用本地函数；MCP 是连接外部服务。
- Function Calling 适合单个应用内部；MCP 适合多个应用共享外部能力。
- MCP 更重，Function Calling 更轻。

**金句**：**Function Calling 是 LLM 怎么调函数，MCP 是怎么让 LLM 安全地调用外部系统。**

### 多 Agent 协作有哪些形式

三种常见形式：
1. **顺序接力**：A 做完交给 B，像流水线，步骤清晰；
2. **主从分配**：一个 Manager 拆任务，多个 Worker 并行做；
3. **圆桌讨论**：多角色对同一个问题讨论迭代，比如架构师 + 测试 + 安全。

**金句**：**多 Agent 不是堆数量，是任务拆清楚、状态管明白、冲突有规则。**

### Agent 怎么评估

不看 LLM 说得不流畅，看任务是不是真正完成。

四个维度：
1. 任务完成度：最后结果解决问题没有；
2. 工具调用正确性：选对工具、参数合法；
3. 输出质量：是不是简洁、无幻觉；
4. 成本效率：token、交互轮数、响应时间。

**金句**：**评估 Agent 不是评 LLM，是评一个能自己调工具完成任务的系统。**

---

## 八、红色技能（被问到就这么说，不主动提）

| 被问到 | 回答 |
|--------|--------|
| Seata / 分布式事务 | “我学过 AT 模式的前后镜像和自动回滚原理，但没在生产深度用过。我真正做过的业务一致性保障是大宅门的 5 分钟主动对账 + 微信回调幂等。” |
| Dubbo / ZooKeeper | “Dubbo 是 RPC 框架，ZK 做服务注册协调，我理解原理。我上一个项目用的是 Spring Cloud + Nacos。” |
| RocketMQ | “RocketMQ 特点是延迟消息、顺序消息、分区有序，我了解这些特性。真实使用中我用的是 RabbitMQ 和 Kafka。” |
| QPS 3000+ | “那是我基于 Caffeine + Redis 多级缓存做的理论估算，没有在生产环境压测。我真正做过的性能优化是大宅门的分级缓存和状态位查询优化。” |
| ELK | “我学过 ELK 的日志采集、索引、可视化原理，但没有深度部署过。我真正做过的日志和监控是大宅门的 Sentry + PostgreSQL 慢查询日志。” |
| Gateway / Nacos | “我学习和实践过 Gateway 过滤器链和 Nacos 服务注册，了解原理。但我没有大规模生产运维经验，主要做的是应用层开发。” |

### 红色技能核心策略
不主动提，被问也不回避；先讲清楚概念，然后立马拉回自己做过的事（大宅门对账、权限、PaiAgent 工具系统）。

---

## 九、离职原因 + 反问

### 离职原因
>“前一份工作让我对业务运营有了很深的理解，但我更想回到技术方向，尤其是 AI Agent。现在工作偏销售和运营，技术成长空间有限。我希望找到一家能长期做技术、有 AI 落地场景的公司。”

### 反问准备 3 个
1. 你们团队目前 Agent 落地到什么阶段？主要在哪些业务场景？
2. 如果我来，主要负责哪块？是新的 Agent 应用开发还是现有系统的 AI 化改造？
3. 团队的技术栈偏 Python 还是 Java？对 Agent 框架选型有什么倾向？

---

## 十、今晚 3 小时怎么背

### 19:30-20:30 第一轮：读熟 4 个项目

目标：自我介绍 + 4 个项目能合上稿子顺利说出来。

1. 先读 2 遍整个文档，重点看万能框架 + 4 个金句。
2. 自我介绍合稿子说 3 遍，录音。
3. 每个项目把“一句话定位 + 3 个要点 + 1 个金句”说 3 遍，录音。

### 20:30-21:30 第二轮：随机练习

目标：练习把不同问题拉回到自己的项目。

随便抽 10 个简历上的点，用这个开头：
> “这个问题我在项目里遇到过，我是这么处理的……”

然后直接套上面的万能框架：一句定位 → 拉回项目 → 3 个要点 → 1 个金句。

### 21:30-22:30 第三轮：录音自检

目标：发现卡壳点，做一张明早关键词小纸条。

1. 回听录音，找 3-5 个卡壳点。
2. 每个卡壳点写成 3-5 个关键词，比如：
   - 大宅门 → “17 位状态位 + 奖励独立 + 幂等 + 5 分钟对账”
   - AI 直查 → “三层分流 + 意图匹配 + 直查数据库 + 防幻觉”
   - PaiAgent → “DAG + LangGraph + ReAct + Tool Registry + MCP”
   - PaiCLI → “ReAct + Tool Registry + MCP + 记忆 + 快照”
3. 明早起床只看小纸条，不再看大文档。

---

## 十一、明早出门前 1 小时

1. 说 1 遍自我介绍录音。
2. 看 1 遍小纸条上的关键词。
3. 选 1 个最熟的项目故事作为“举例候选”，面试官让你举例时直接用。
4. 心态：面试官问的不是你知道多少，而是你能不能在压力下把做过的事诲清楚。

---

## 最后检查清单

今晚必须完成：
- [ ] 自我介绍能合上稿子说 1 遍
- [ ] 大宅门能合上稿子说 1 遍
- [ ] AI 直查能合上稿子说 1 遍
- [ ] PaiAgent 能合上稿子说 1 遍
- [ ] PaiCLI 能合上稿子说 1 遍
- [ ] 录音自检，生成小纸条

明早：只看小纸条，不背新内容。
